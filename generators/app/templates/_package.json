{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "<%= projectDescription %>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/<%= githubUsername %>/<%= projectName %>.git"
  },
  "author": "<%- authorInfo %>",
  "license": "MIT",
  "keywords": [
  ],
  <%if (projectType === 'npm-package-js') { %>
  "main": "dist/index.js",
  <% } %>
  <%if (projectType === 'web-app-react') { %>
  "main": "dist/bundle.js",
  <% } %>
  "scripts": {
    <%if (linting) { %>
    "lint": "eslint src<%if (projectType === 'web-app-react') { %> webpack.config.js<% } %>",
    <% } %>
    <%if (testing) { %>
    "test": "mocha src/**/*.test.js --compilers js:babel/register",
    "test:watch": "npm test -- -w",
    <% } %>
    <%if (projectType === 'npm-package-js') { %>
    "build": "babel src/index.js -o dist/index.js -s",
    "dev": "npm run build -- -w",
    <% } %>
    <%if (projectType === 'web-app-react') { %>
    "build": "webpack",
    "dev": "webpack-dev-server",
    <% } %>
    "prepublish": "<%if (linting && testing) { %>npm run lint && npm test && <% } %><%if (linting && !testing) { %>npm run lint && <% } %><%if (testing && !linting) { %>npm run test && <% } %>npm run build"
  },
  <%if (linting || testing) { %>
  "pre-commit": [
    <%if (linting && testing) { %>
    "lint",
    "test"
    <% } %>
    <%if (linting && !testing) { %>
    "lint"
    <% } %>
    <%if (testing && !linting) { %>
    "test"
    <% } %>
  ],
  <% } %>
  "dependencies": {
  },
  "devDependencies": {
    <%if (linting || testing) { %>
    "pre-commit": "1.x.x",
    <% } %>
    <%if (testing) { %>
    "mocha": "2.x.x",
    <% } %>
    <%if (linting) { %>
    "eslint": "1.x.x",
    <% } %>
    <%if (projectType === 'web-app-react') { %>
    "eslint-plugin-react": "3.x.x",
    "babel-core": "5.x.x",
    "babel-loader": "5.x.x",
    "react": "0.13.x",
    "react-hot-loader": "1.x.x",
    "file-loader": "0.8.x",
    "webpack": "1.x.x",
    "webpack-dev-server": "1.x.x",
    <% } %>
    "babel": "5.x.x"
  }
}
